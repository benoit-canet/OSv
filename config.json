{
   "modules": {
        "osvinit": {
            "type": "direct-dir",
            "path": "${OSV_BASE}/modules/cloud-init/"
        },
        "mgmt": {
            "type": "direct-dir",
            "path": "${OSV_BASE}/mgmt/"
        },
        "nfs-tests": {
            "type": "direct-dir",
            "path": "${OSV_BASE}/modules/nfs-tests/"
        },
    "repositories": [
        "${OSV_BASE}/apps",
        "${OSV_BASE}/modules"
    ]
   },
   "default": [ "cloud-init", "httpserver" ]
}
